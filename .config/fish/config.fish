# Prompt
set theme_display_git_untracked no
. $functions/fish_prompt.fish

# Aliases
alias urlab-ssh "ssh -t azog.urlab.be ssh"
alias ls "ls --color"
alias ll "ls -l"
alias la "ll -A"
alias lll "tree -C"
